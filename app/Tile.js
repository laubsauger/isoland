/**
 * Tile object
 * @param {Integer} x
 * @param {Integer} y
 * @param {Integer} level
 * @constructor
 */
var Tile = function(x, y, level) {
    this.x = x;
    this.y = y;
    this.level = level;
};